interface:
  display_name: "Manuscript Workflow"
  short_description: "Evidence-first scientific manuscript workflow"
  default_prompt: "Use $manuscript-workflow to build or audit a journal-style manuscript from my local results and reference papers."

policy:
  allow_implicit_invocation: true
